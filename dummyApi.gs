function processForm(form) {
  return form.action;
  switch(form.action){
    case 'newArticle':
      return form.action;
    case 'editArticle':
      return form.action;
  }

  return 'Form submitted successfully!';
}